int KH = 25;
int KW = 25;

void setup(){
  size(200,200);
}

void draw(){
  background(#A9FCF1);
  
  smooth();
  
  //noStroke();
  strokeWeight(2);
  
  //rect(200/2 - 25/2,200/2 - 25/2,25,25);
  
  //rect(width/2 - KH/2,height/2 - KW/2,KH,KW);
  
  fill(#F7C41B);
  
  rectMode(CENTER);
  rect(width/2,height/2,KH,KW);
  
  fill(#56F71B);
  
  ellipseMode(CENTER);
  ellipse(width/2,height/2,10,10);
}
